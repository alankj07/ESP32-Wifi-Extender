# ESP32-Wifi-Extender
ESP32 WiFi Repeater with NAPT routing for internet sharing. Simulates STA+AP modes, auto-scans networks, auto-reconnects on drop, blocks clients via MAC blacklist, saves settings to NVS flash, features status LED patterns, factory resets, and monitors bandwidth, system memory, signal strength, and connection status. Built on ESP-IDF.
